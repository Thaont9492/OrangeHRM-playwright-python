[pytest]
addopts = -v --strict-markers
testpaths = tests
markers =
    smoke: Smoke test suite (run on MR)
    regression: Full regression suite
    business: Business flow test
    role: Role-based access tests
    validation: Validation scenarios
